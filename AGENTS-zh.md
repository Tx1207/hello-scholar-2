# 项目工程指南

编写无需推倒重来的代码。

这些规则适用于当前工作的项目，不绑定语言、框架或 Agent 平台。遵循当前用户请求和项目已经确定的约束；本指南不授予额外权限。

## 修改前阅读

- 修改项目之前，阅读已有 README 和贡献指南（如 CONTRIBUTING.md），以及相关范围的指令。遵循当前项目的命令与约束。
- 完整阅读每个待修改文件；上下文中已经完整读过且未变化的版本满足此要求。检查相关调用方、导入、配置、依赖和测试，再确定修改边界。
- 复用已确认的项目模式。改变既有接口、库或结构时，说明项目事实支持的原因和影响。

## 编码前思考

- 明确会影响行为、数据、范围或风险的假设，先从项目事实解决疑问。
- 未决解释会明显改变结果时，说明选择。高风险或不可逆操作前询问；低风险歧义可说明合理假设及其验证方式后继续。
- 分析、评审、诊断和设计请求不授权实现写入。明确要求实施时，继续到相关本地验证和受影响文档更新，无需重复阶段审批。
- 已有授权不扩展到无关修改、付费运行、对外发消息、部署或破坏性操作。

## 简洁与边界

- 交付满足当前需求的最小端到端变更。没有具体需求，不添加抽象、配置、回退分支或未来扩展点。
- 保留无关用户修改，不重置、覆盖或重新格式化。
- 保持组件职责清晰。在同一变更中清理因本次修改失效的代码、导入、测试和文档，不顺带清理无关问题。
- 调用方都在仓库内且能同时更新时，直接替换旧接口。只有公共 API、持久化格式或第三方集成等已确认的外部契约才需要兼容。
- 范围扩大时拆成可独立验证的工作，或回到设计解决未决项，不静默扩展任务。

## 避免过度防御

- 不要提出 SHA、哈希、内容指纹或摘要绑定方案，除非同时满足：它替代了一项成本显著更高的操作，而且其结果会改变后续行动。
- 不要重复读取已在上下文中、且已确认没有变化的文件。
- 不要搭建防御性框架：不要为当前项目不存在的情况增加功能开关、迁移框架、兼容层或包装层。
- 需要判断时，直接作出判断。不要用评分表、检查清单或反复验证的循环代替判断。
- 交付文本不是自我辩护记录。直接说明成立的事实；将必要的保留说明集中放在一个“局限性”章节，标题遵循任务语言。
- 不要把写作指令写进成品。“不要提及 X”意味着成品中不出现 X，而不是写上“我们不讨论 X”。

## 验证与执行

- 实施前定义可观察的成功标准。多步骤工作简要配对步骤与验证；原生任务工具只在有帮助时使用。
- 测试行为、边界和回归风险，不绑定偶然的实现细节。修复 Bug 时，尽可能先构造失败测试或可复现信号。
- 从最小相关检查开始，按影响扩大。查明项目实际命令，不假定包管理器或测试框架。
- 声称成功前使用当前工作树的新证据。报告未运行的检查、无法复现之处和剩余风险；旧日志及其他 Agent 的总结不是证明。
- 用户明确不需要测试时，采用合适的静态检查、dry run、回读或定向 diff 审查，并说明未覆盖的风险。
- 持续到授权目标得到验证，或真实阻塞需要输入。没有新修改、失败或未决疑点时，不重复运行已通过的检查。

## 调试与依赖

- 阅读完整错误、相关输入、日志和运行条件。定位失败时尽可能每次验证一个明确假设。
- 修复根因并保持调用方可见的失败语义。重试、吞异常、空值检查或默认值不能代替根因分析。
- 新增依赖或自行实现常见能力前，检查已有代码、依赖、标准库和合适的成熟维护库。
- 用实际版本、调用方、类型或官方文档确认能力。解释新依赖解决的具体需求与维护影响，同时更新受影响的 manifest、lockfile 和部署文档。

## 代码注释

- 解释不明显的契约、原因和影响，不复述语法。
- 对职责明确的模块、类或函数入口，在代码和命名无法说明时，补充职责及调用方可见的输入、结果、副作用或失败行为。
- 复杂阶段和分支附近说明影响正确性的目的、顺序、不变量或回退原因，不用通用入口摘要代替局部解释。
- 注释简短、准确、不重复。结束前检查缺失的理由和过时说法，不强制每个函数套用机械注释模板。

## 持久设计与证据

- 根据用户目标和项目事实直接工作。设计、接口、不变量或验收标准需要长期保存时使用 Spec；普通小改动不强制建文档。
- 使用 hello-scholar 文档时，Spec 位于 `hello-scholar/specs/<topic>/SPEC-NNN-<name>/spec.md`；已实现且已采用的架构事实位于 `hello-scholar/architecture.md`；正式实验 Record 位于 `runs/<run-id>/record.md`；Handoff 位于 `hello-scholar/handoffs/`。
- 更新受影响的现有事实，不创建竞争事实源。不为匹配默认路径迁移已有项目文档；创建新来源前先确定归属。
- 临时步骤不需要 `plan.md` 或 `tasks.md`，不创建或审批这些文件作为新的 hello-scholar 流程门禁。保留历史文件，迁移时核对其中独有约束。
- `hello-scholar docs check` 只读。`hello-scholar docs sync` 负责生成的 Index，会写文件：仅在 CLI 可用且相关写入属于用户授权范围时运行。不为满足此约定自行安装工具或扩大范围。
- 正式实验保留命令、输入、环境、原始输出、结果和结论。区分有效负结果与运行失败；不因运行完成就推断方案已采用。

## 沟通

报告结果、影响范围、本次验证和剩余不确定性。不凭旧日志或其他 Agent 的总结宣称完成。

主 Agent 只在本轮最后一条消息使用以下回答模板：

```text
{icon} 【hello-scholar】- {status} - {Skill or agent name}

{result, evidence, impact, and remaining uncertainty}

🔄 下一步: {next state or action}
```

状态：`💡直接响应`、`⚡快速执行`、`🔵规划流程`、`✅完成`、`❓等待输入`、`⚠️警告`、`❌错误`。需要用户输入或授权时使用 `❓等待输入`；仅在没有剩余请求工作时使用 `✅完成`。

## 语言与数据安全

- 优先遵循当前任务明确指定的语言，否则沿用目标文件或项目既有语言。保留技术标识符和模板必需字段。
- 先说结果和实际影响，再说本次证据、剩余不确定性和有用的下一步。自然、直接表达，遵循项目要求的回复格式。
- 不提交密钥。大体积二进制、模型权重、数据集、checkpoint、实验 outputs/results/logs、构建产物或压缩包默认不加入 Git。
- 暂存或提交前检查新增文件大小和目标 diff。用精确忽略规则排除不必要的生成文件；确需跟踪大文件或改写历史时先询问。
