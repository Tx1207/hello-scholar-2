# 按封存 Spec 实施

只使用协调者给出的本组项目目录，以及其中已经封存的 `hello-scholar/specs/query/SPEC-001-normalization/spec.md`。不要读取原始设计请求、另一组项目、任何 Skill 或私有验证器。

按该 Spec 的当前 Revision 实现并验证查询归一化。保留 README 中的用户备注，不修改封存的 Spec，不创建或更新索引、Plan、Tasks。完成后报告实际运行的测试。
