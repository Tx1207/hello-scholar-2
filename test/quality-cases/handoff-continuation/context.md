# 本轮会话事实

正在实现查询归一化。已有 Spec 定义了 normalize_query 的入口，但尚未把刚确定的边界全部写入。

用户最后确定：输出先做 Unicode NFC；首尾空格和 tab 去掉，内部连续 ASCII 空格与 tab 压成一个空格。保留大小写和非 ASCII 空白字符。输入含 CR 或 LF 时抛 ValueError，不把多行输入拼成单行。不要改公共函数名或参数。

此前仅跑过两个已有测试，它们通过，但没有覆盖这些新边界。当前实现还没有完成，不应把旧测试通过当作新需求完成。

README.md 末尾的“用户备注：保留实验术语的大小写。”是用户的修改，必须保留。当前任务只涉及本地实现和验证，不包含发布或提交。

当前请求：保存一份交接，让下一个会话可以直接继续完成这项工作。本轮只保存交接，不开始实现。
